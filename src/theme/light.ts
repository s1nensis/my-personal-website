import { CustomTheme } from "../types/theme";

export const lightTheme: CustomTheme = {
  palette: {
    mode: "dark",
    primary: {
      main: "#3f51b5",
    },
    secondary: {
      main: "#f50057",
    },
    background: {
      default: "#0FF",
    },
  },
};

export default lightTheme;
